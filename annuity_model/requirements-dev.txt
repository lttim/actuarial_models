# Developer / test-only dependencies. Install via:
#   pip install -r requirements.txt -r requirements-dev.txt
# Production / Streamlit-Cloud installs MUST NOT pull this file.
# Pinned to the versions in the M5 dev venv on 2026-04 (Python 3.12).
pytest==9.0.3
pytest-html==4.2.0

# Static-analysis / formatter (mirrored by pre-commit + CI)
# black removed 2026-04: ruff-format is the single source of truth for
# formatting (black-compatible output, no double-formatter conflict).
# When bumping ruff or mypy, also bump the matching `rev:` in
# .pre-commit-config.yaml so local pre-commit and CI use the same checker.
ruff==0.15.12
mypy==1.20.2

# Documentation site (mkdocs --serve / GitHub Pages deploy)
mkdocs==1.6.1
mkdocs-material==9.7.6
mkdocstrings[python]==1.0.4
pymdown-extensions==10.21.2

# Property tests + perf gates + mutation pilot (P4)
hypothesis==6.152.3
pytest-benchmark==5.2.3
pytest-cov==7.1.0
mutmut==3.5.0

# Runtime-Excel parity (P4 -- xlcalculator-based recalc gate)
# PARKED 2026-04: xlcalculator==0.5.0 transitively pulls yearfrac<2, which
# conflicts with the pinned numpy==2.4.4 in requirements.lock. The parity test
# `tests/parity/test_runtime_excel_recalc.py` uses `pytest.importorskip` so it
# auto-skips while this is uninstalled. Restore options (in priority order):
#   1. xlcalculator release that drops or relaxes the yearfrac dep
#   2. Replace with `formulas` or `pycel` (pure-Python evaluators w/o yearfrac)
#   3. Vendor a yearfrac fork that supports numpy 2.x
# Quarterly recurring check: re-test xlcalculator + yearfrac + numpy 2.x
# compatibility to restore the runtime Excel recalc gate.
# Tracking: docs/runbooks/runtime_excel_recalc_gate.md
# xlcalculator==0.5.0

# Security / supply-chain
# When bumping these, also bump the matching hardcoded versions in
# .github/workflows/security.yml (pip-audit / bandit jobs install fixed
# versions to avoid surprise CI failures from new rule sets).
pip-audit==2.10.0
bandit==1.9.4
