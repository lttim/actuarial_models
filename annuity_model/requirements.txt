# Runtime dependencies for pricing_ui.py (Streamlit Cloud: see ../requirements.txt at repo root).
# Pinned to the versions used in the M5 dev venv on 2026-04 (Python 3.12).
# streamlit>=1.41: avoids altair 5+/6 vs streamlit 1.19 vegalite.v4 import crash (uv resolution).
# Test/dev tools (pytest, pytest-html, ruff, mypy, ...) live in requirements-dev.txt.
numpy==2.4.6
pandas==3.0.3
pyarrow==23.0.1
altair==6.2.2
streamlit==1.59.2
matplotlib==3.11.0
openpyxl==3.1.5
defusedxml==0.7.1
