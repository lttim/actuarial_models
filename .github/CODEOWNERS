# CODEOWNERS for Code_Sandbox.
# https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-features/customizing-your-repository/about-code-owners
#
# Branch protection on `main` requires a review from the matching owner below
# for every PR.
#
# == Solo-owner status (2026-04) ==
# Today the repo is solo-owned by @lttim. The phase-5 backlog called for
# adding a second human/team CODEOWNER so branch protection on `main` can
# require a real second-pair-of-eyes review (otherwise self-review of every
# parity-critical change is unenforceable). This is BLOCKED on a real second
# reviewer existing -- it cannot be solved by editing this file alone.
#
# Two extension hooks are in place for the moment that's no longer true:
#
#   1. The placeholder team handle `@lttim/actuarial-reviewers` is documented
#      below as a commented TODO. The day a second human/team reviewer is
#      onboarded, uncomment those lines, create the team in GitHub with that
#      slug, and add the new member(s); branch protection's "require review
#      from CODEOWNERS" rule will then transparently start enforcing
#      second-pair-of-eyes on every parity-critical PR.
#
#   2. annuity_model/docs/CODEOWNERS_RATIONALE.md tracks WHY each path is
#      flagged so the second owner has context day one.

# --- Default owner for everything ---
* @lttim
# TODO(second-owner): * @lttim @lttim/actuarial-reviewers

# --- Parity-critical actuarial surface ---
# Any change to engine math, ALM ladder, or the Excel formula validator MUST
# be reviewed by an actuarial owner. Documented in
# annuity_model/docs/CODEOWNERS_RATIONALE.md (added in Phase 3).
/annuity_model/pricing_projection.py            @lttim
/annuity_model/term_projection.py               @lttim
/annuity_model/rila_projection.py               @lttim
/annuity_model/alm_excel_ladder.py              @lttim
/annuity_model/excel_workbook_validator.py      @lttim
/annuity_model/product_registry.py              @lttim
/annuity_model/parity_constants.py              @lttim
# TODO(second-owner): once @lttim/actuarial-reviewers exists, append it to
# every parity-critical line above so branch protection actually requires
# a second actuarial signoff on engine / ladder / validator changes.

# Excel builders write the workbook structure that the validator gates.
/annuity_model/build_pricing_excel_workbook.py  @lttim
/annuity_model/build_term_excel_workbook.py     @lttim
/annuity_model/build_rila_excel_workbook.py     @lttim
/annuity_model/recalc_excel_shared.py           @lttim
/annuity_model/product_excel.py                 @lttim

# --- Load-bearing internals (P0 hardening 2026-04) ---
# These files were previously only covered by the catch-all `*` rule above
# but are critical to the platform contract -- a silent change to any of
# them can break parity, the UI, or the dispatch surface. Promoting them
# to explicit lines means a CODEOWNERS-required review before merge and a
# clear signal to reviewers that these paths are load-bearing.
/annuity_model/liability_layouts.py             @lttim
/annuity_model/liability_dispatch.py            @lttim
/annuity_model/data_registry.py                 @lttim
/annuity_model/pricing_ui.py                    @lttim
/annuity_model/streamlit_app.py                 @lttim
/annuity_model/pricing_run_form_state.py        @lttim
/annuity_model/_logging.py                      @lttim
/annuity_model/_observability.py                @lttim

# Parity contracts and the parity test suite are the model's public guarantees.
# Tightening or relaxing tolerances always needs an actuarial sign-off.
/annuity_model/docs/model_parity_contract.md    @lttim
/annuity_model/docs/rila_parity_contract.md     @lttim
/annuity_model/docs/parity_test_checklist.md    @lttim
/annuity_model/tests/parity/                    @lttim
# Golden ModelCheck snapshots: byte-exact pinned per-product metrics.
# Touching these JSONs is the same as widening a parity tolerance and
# MUST be reviewed by the actuarial owner. The test that gates them is
# tests/parity/test_golden_modelcheck.py.
/annuity_model/tests/parity/golden/             @lttim
# mutmut PR-gate thresholds: raising any cap weakens the mutation gate
# on parity-critical engines. Same review discipline as parity tolerances.
/annuity_model/mutmut_thresholds.toml           @lttim
# RUN_KEY drift ratchet: raising any per-file baseline reverses the
# pricing_ui.py migration progress and must be reviewed.
/annuity_model/tests/run_state_key_baseline.json @lttim

# --- Agent guidance ---
# Editing AGENTS.md or the .cursor/rules/ files changes how every future
# Cursor session behaves. Treat as load-bearing.
/AGENTS.md                                      @lttim
/annuity_model/AGENTS.md                        @lttim
/annuity_model/.cursor/                         @lttim
/annuity_model/.cursorrules                     @lttim

# --- CI / control plane ---
/.github/                                       @lttim
/.pre-commit-config.yaml                        @lttim
/annuity_model/pyproject.toml                   @lttim
/annuity_model/requirements*.txt                @lttim
/annuity_model/requirements.lock                @lttim
