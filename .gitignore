# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/

# --- Virtual environments (any common name) ---
.venv/
.venv_ci/
venv/
env/
.env
.envrc
.python-version

# --- Generated outputs (regeneratable from the engines) ---
reports/
model output/
streamlit output/
tmp_*.csv
# Local smoke artifacts (deep_smoke, portfolio CLI acceptance runs, etc.)
annuity_model/.smoke/
# mkdocs build output (regenerated by `mkdocs build` / docs.yml)
annuity_model/site/

# --- Editor / IDE ---
.idea/
*.swp
*.swo

# --- macOS ---
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes
.fseventsd
.DocumentRevisions-V100
.TemporaryItems

# --- Windows ---
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
Desktop.ini
$RECYCLE.BIN/
*.lnk

# --- Linux ---
*~
.directory
.Trash-*

# --- Streamlit local secrets ---
.streamlit/secrets.toml

# --- Cursor chat handoffs (personal session artifacts) ---
# The protocol rule and README are tracked; the generated handoffs are not.
.cursor/handoffs/*.md
!.cursor/handoffs/README.md

# --- Actuary SME reviews (personal session artifacts) ---
# The protocol rule and README are tracked; the generated verdicts and
# transient evidence pack are not.
.cursor/actuary-reviews/*.md
!.cursor/actuary-reviews/README.md

# --- Autonomous AI team run packets ---
# Generated by annuity_model/scripts/agent_preflight.py. Commit excerpts into
# PR/release evidence when useful, but keep raw session packets local.
.agent-team-runs/
